# 更新日志

## 未发布

### 新增
- 公网备案页脚支持公安备案号、官方图标及对应查询链接，与 ICP 信息共同显示并适配窄屏换行；支持单独发布前端镜像。
- 新增生产加密备份与隔离恢复演练：定时保存数据库、Redis、Qdrant、应用文件和必要配置，支持 OSS 异机保存、上传后下载校验及历史 Provider 密钥恢复验证。
- 新增轻量运维告警，覆盖服务依赖、备份失败与超时、磁盘和证书有效期；补充云监控资源阈值、邮件验证和恢复操作说明。
- 公网部署可在构建时配置 ICP 备案号，登录页和应用页面底部统一展示工信部备案链接；未配置时本地界面保持原布局。
- Embedding 新增服务端 OpenAI 兼容 API 接入，可配置百炼模型、维度与批量上限；请求失败直接返回脱敏错误，拒绝不完整或维度异常的向量，不加载本地模型兜底。
- Qdrant 新增 Cloud API Key 鉴权与过滤字段索引初始化；检索和健康检查只读取配置集合，管理员同步时才创建缺失集合和准备索引。
- 新增阿里云轻量内测部署说明，记录配置覆盖顺序、SSH 隧道访问、数据迁移边界，以及公网开放、备份恢复和容量验证的后续步骤。
- 大模型配置新增 OrcaRouter 可选 Provider 预设，默认使用官方 OpenAI-compatible 端点和免费路由 `orcarouter/free`，并在配置页与 README 提供项目推广注册链接；现有 Provider 与当前启用配置保持不变。

### 变更
- 阿里云部署启用正式域名 HTTPS 和 HTTP 自动跳转，保留邮箱验证认证、管理员题库维护、外部向量服务及现有数据；部署分支同步主分支更新，并补充发布版本、回滚备份、正式域名用户手工验收和后续验证边界说明。
- 新增外部服务 Compose 覆盖、本地部署脚本选项和两环境配置示例，可移除本地向量模型与 Qdrant 服务；本地保留完整题库功能，云端普通用户只读、管理员维护公共题库，并支持首次管理员配置。切换时使用独立集合，默认关闭启动导入与自动补建，数据迁移需显式执行。
- 云端部署新增小内存资源预算与预构建镜像覆盖，支持在开发机完成构建后传至 ECS；无域名时默认经 SSH 隧道访问，外部服务模式统一通过前端代理 API。
- 题库构建的分类规划、知识原子生成、质量监督和修复统一使用可恢复调用协议，兼容旧检查点及坏响应审计记录；领域节点继续负责候选幂等、人工决定和终审发布。
- 面试下一问统一采用稳定规则：由阶段状态机推进，在技术 / HR 阶段按当前岗位检索，并使用真实最新回答判断补救、换题或自然推进；规则动作直接结构化返回，不再解析中文 Prompt。
- 面试轮次计划精简为阶段、动作、Prompt、证据上下文及证据 / 消费原子两组 ID；补救证据可进入成功轮次的覆盖与复盘而不用于本场排重，新轮次固定记录 `RULE`，历史编排字段与旧记录继续兼容读取。
- AI Mentor 改为按当前可见岗位独立生成诊断、风险提醒、行动建议和知识覆盖；岗位切换、缓存与面试结束后的后台刷新均使用结构化岗位 ID，不再跨岗位聚合训练记录。
- Windows 本地 Docker 部署统一使用可重试脚本和持久 Maven 构建缓存；Redis、Qdrant 默认只在容器网络开放，避免 Docker context 路径转换与系统动态端口保留导致重建失败。

### 修复
- 运维通知补齐日期与 Message-ID 邮件头，测试邮件改用清晰的中文标题，并明确发送端回执与收件箱实收的验证边界。
- 运营后台首次进入时自动加载统计，避免已登录管理员仍看到需要登录的空态提示。
- HTTPS 入口恢复 HTTP 自动跳转，API 直接转发后端并覆盖外部伪造的来源 IP，避免所有访客共享代理 IP 限流；访问日志不再记录可能携带 SSE token 的查询参数或 Referer。
- 修复已发布题目创建草稿后连续编辑、批量发布或归档时发生历史版本号冲突的问题；版本统一按当前记录与历史最大号递增，内容更新不再覆盖版本计数。
- 修复题库监督或修复复用有效响应时，业务写入失败被误判为内容错误并再次调用模型的问题；响应或完成检查点保存失败会保留恢复依据，失去执行租约后停止继续调用或应用结果。
- AI Mentor 的 Provider 返回无效内容或请求失败时改为展示岗位级加载失败，不再错误显示为“暂无面试数据”；同时为 Mentor 请求使用与后端模型调用匹配的专用超时。

### 移除
- 移除比赛版面试 Agent 的 Tool Calling 规划、自动回退、专用线程池与超时会话状态、`APP_INTERVIEW_AGENT_*` 配置，以及文字 / 视频面试决策状态条和 SSE `orchestration` 事件。

### 文档
- README、领域上下文与 RAG 链路说明统一为稳定规则主线；新增 ADR 0004，原 Agent 决策保留为历史资料，移除 GOAI 比赛演示指南。

## v1.5.0 - 2026-08-13

### 新增
- 知识库 / 题库工作台新增受控文档入库流水线，私有题库 owner 与公共 starter 题库管理员均可上传 PDF、DOCX、TXT、Markdown 文档，使用自己的 active LLM Provider 依次生成知识原子并执行独立质量监督。
- 题库构建支持自动轮询生成、监督、终审、发布和索引状态；默认只将监督异常交给人工处理，异常清零后由 owner 一次终审并显式发布整批知识原子。
- 构建按已完成分块、已保存模型响应和已落库候选恢复；进程中断后先复用响应补齐部分候选，远端结果不确定时不会自动重调，只有用户显式点击重试才授权再次调用。
- 新增受限文档解析和构建批次存储，限制文件类型、大小、数量、文本量、分块数和候选数；删除尚未进入终审且非运行中的批次时同步清理其源文件，已发布批次保留来源记录。
- 公共题库管理员可在三个 starter 岗位使用同一文档入库、机器监督和批次终审流程；构建批次、源文档、候选与作业仍按发起管理员私有隔离，只有终审发布后的原子进入公共题库。
- 题库流水线新增最多两轮的自动修复与重新监督：监督异常会先由受限修复助手依据原文调整，修复历史、字段前后差异、轮次和失败状态均可审计；用户也可对仍需关注项补充要求后再次请求修复。
- 智能构建页新增“解析文档—生成原子—质量监督—自动修复—最终审核—发布入库”六步状态展示，最终审核页提供“需关注 / 助手修改 / 全部候选”视图和可读的字段级修改说明。
- 文档构建未指定分类范围时新增整批知识领域自动规划阶段，生成、修复和人工终审统一受分类目录约束，并在批次详情展示最终目录。

### 变更
- 知识库 / 题库工作台调整为“概览、智能构建、终审发布、题库原子”四标签布局；终审页使用桌面三栏工作台和移动端分栏切换，不新增独立一级导航。
- 机器质量状态与人工审核状态分离：`AUTO_PASS` 只允许候选进入批次终审，不能自动发布；`NEEDS_HUMAN` 先进入有限轮自动修复，耗尽后默认留存不发布；`AUTO_REJECT` 默认排除。终审只发布明确选择的合格候选，先在 MySQL 形成可审计草稿并发布，再以可重试步骤同步 Qdrant。
- 应用内题库构建只对私有知识库 owner 或公共知识库管理员开放，严格使用发起账号启用的 Provider；没有 active Provider 时阻止构建，不使用系统兜底密钥，也不向其他管理员暴露构建工件。
- 人工编辑或应用建议补丁后，必须满足标准难度、核心原理和至少两条追问路径才可标记审核通过；修改已发布原子只创建草稿修订，新修订发布并同步成功后再归档旧版本。
- 旧的构建批次候选包导出已停用，页面隐藏旧 JSON 导入入口；通用 JSON 导入现在只能形成 `NEEDS_REVIEW` 草稿，不能伪造审核通过。受控流水线通过带 revision 校验的一次批次终审发布选中的合格候选，未解决项不再拖住整批。
- 智能构建与题库原子页暂时隐藏旧 JSON 导入入口但保留恢复代码；原子列表默认改为每页 10 条，“分类范围”留空时自动识别，手动选择时作为候选可用分类的严格范围。
- 外部导入与发布统一要求可追溯来源和至少一条原文摘录；RAG 检索只返回已发布、人工审核通过且 Qdrant 同步成功的原子，索引失败项在恢复前不会进入面试上下文。
- 最终审核默认展示与实际提交完全一致的“待发布”候选；未保存编辑会阻止刷新、切换和发布，候选修改通过审核 revision CAS 防止并发覆盖。
- 最终审核页将机器通过和人工确认的候选自动汇总为发布集合，提供“一键发布全部”批次操作；自动通过项不再要求逐条点击确认，需关注、自动淘汰和人工拒绝项仍不会混入发布。

### 修复
- 修复新文档构建在未选择分类时把所有候选默认归为“通用”、导致原子分类与岗位知识覆盖语义不一致的问题；分类建议也改为读取当前岗位已有覆盖和原子，不再展示全局 Java 固定列表。
- 修复流水线发布完成后只刷新原子数量、不刷新岗位覆盖数据，导致概览暂时误报“暂无已发布题库分类”的问题；发布、归档和切回概览都会重新读取覆盖结构。
- 修复候选审核页自动收起岗位栏后，主工作区仍落入零宽网格列而导致标题、按钮和三栏审核内容被压成竖排的问题。
- 修复候选队列没有高度上限、长批次持续撑高整页的问题；桌面端和移动端均使用独立纵向滚动区。
- 修复已发布构建仍可删除源文件并留下悬空来源引用的问题；进入终审发布的批次改为保留可审计来源记录。
- 修复公共岗位加载私有构建记录导致重复“无权访问知识库”、并中断题库原子和覆盖数据刷新的问题；公共岗位现在明确显示“公共题库可维护”。
- 修复快速切换岗位时旧请求覆盖当前题库原子、知识覆盖、构建记录、候选列表、编辑弹窗或 JSON 校验结果的问题。
- 修复知识覆盖命中总数为 0 时仍绘制等分彩色玫瑰图的误导表现，零命中改为中性空态。
- 修复模型以普通文本返回来源证据，或以 `snake_case` 返回追问路径时题库构建批次直接失败的问题；兼容文本、对象、数组证据和常见字段命名，并通过明确 JSON Schema 约束模型输出。
- 修复作用域 JSON 重复导入已发布稳定 ID 时直接覆盖线上原子的问题；现在保留已发布版本并创建新的草稿修订。
- 修复人工修改自动通过候选后仍可沿用旧监督结论的问题；保存修改会撤销自动通过资格并要求人工接受或拒绝。
- 修复终审作业可能在数据库事务提交前被调度的问题；作业与构建状态提交成功后才进入执行线程，索引失败重试不会重复生成或重复导入知识原子。
- 修复人工已拒绝但机器曾自动通过的候选仍可能被构造请求重新发布的问题；人工拒绝在终审入口和执行层均具有最高优先级。
- 修复候选审核与批次终审并发时旧候选列表可能套用新时间戳通过校验的问题；改用单调递增 revision 和数据库条件 claim。
- 修复终审导入丢失来源文件关联、Qdrant 点已写入但 payload 仍标记 `PENDING`、索引补偿重复 upsert 已同步原子，以及服务重启后过期 RUNNING 作业可能长期无人恢复的问题。
- 修复上传前按 PDF 二进制体积虚报分块与调用规模、构建刚启动时作业阶段仍短暂停留排队、监督结果计数只在整批结束后更新，以及已处理异常仍显示在终审徽标的问题；分块数改为后端解析后展示，作业状态和未决监督计数随实际处理实时刷新。
- 修复岗位概览把当前 20 条分页中的已发布原子误报为全部已发布向量的问题；现在单独按 `PUBLISHED + SYNCED` 查询全量总数。
- 修复模型首次返回无效、空或字段契约不完整的知识原子 JSON 后，显式重试仍永久复用同一坏响应的问题；整批候选先完整校验再落库，自动恢复继续禁止重复付费，只有用户重试增加授权版本后才保留坏响应审计并重新调用。
- 修复长时间生成、监督、发布或索引超过作业租约后可能被并发重新领取的问题；每次领取使用唯一执行令牌，运行期间按分钟续租，完成、失败和进度更新继续按令牌隔离旧执行线程。
- 修复公共题库监督检索把 `owner_user_id = null` 当成“不限制 owner”的问题；MySQL 与 Qdrant 现在都显式限定公共原子 owner 为空，异常历史数据不会混入监督上下文。
- 修复同一 LLM 配置记录修改 Provider、连接端点、模型或 temperature 后仍复用旧构建批次的问题；构建幂等键现在绑定完整的非密钥运行快照。
- 修复文档解析发生在上传请求内、导致页面看不到解析进度且解析失败没有批次记录的问题；原始文件保存后由异步作业进入 `PARSING` 阶段完成提取与分块。
- 修复通用 JSON 导入把所有内容无条件标记为 `PASS`，可重新包装人工拒绝候选绕过终审的问题；外部导入改为待审核，终审使用独立受信路径。
- 修复并发点击重试缺少状态和旧重试次数 CAS、作业失去执行令牌后仍可能继续付费调用的问题；重试、续租和进度更新现在按唯一执行令牌隔离。
- 修复构建只保存 Provider/模型而未固定端点与 temperature，排队后配置漂移仍执行旧批次的问题；新增不含 API Key 的运行指纹并在生成、监督和修复前校验。
- 修复质量监督请求缺少响应检查点、网络超时可能被当作内容异常并自动追加修复调用的问题；监督与修复均先持久化调用和原始响应，未知结果只能由用户显式重试授权重调。
- 修复陈旧 worker 可能覆盖人工拒绝、手动修复误处理未选择候选以及修复失败后锁死终审的问题；机器更新仅写白名单字段并要求候选仍为 `PENDING`，手动修复严格限定目标且失败后恢复终审状态。
- 修复修复助手返回空响应时触发空指针、失败候选残留 `RUNNING / REPAIRED` 且页面仍提示正常完成的问题；空响应统一转为可重试的结构错误，只有用户再次明确提交才会重调，失败候选和批次统计会恢复为可继续终审的状态。
- 修复终审发布完成后异步作业的审计阶段仍停留在 `PUBLISHING` 的问题；成功写入数据库并同步索引后，作业与构建现在都会持久化为 `PUBLISHED`。
- 修复删除私有岗位后仍遗留构建候选、源文件和本地原始文档的问题；删除现在会拒绝活动作业，并在移除岗位前清理其私有构建工件。

## v1.4.0 - 2026-08-04

### 新增
- 技术面新增有边界的单轮 Tool Calling Agent，可在服务端固定作用域内调用岗位知识检索、当前岗位简历证据和同岗位学习覆盖三个只读工具，并在深挖、补救、换题、项目追问、进入 HR 和保持阶段之间选择下一问动作。
- 文字与视频面试新增安全的 Agent 决策状态条；SSE `orchestration` 事件只返回模式、动作、公开摘要、友好工具名和白名单降级分类。
- `interview_turn` 新增编排模式、决策动作和脱敏决策元数据，支持复盘 Agent、稳定规则与自动回退路径。
- 新增题库能力接口，前端可根据后端权限显示管理员公共题库维护入口，并拦截普通用户直接访问维护页。
- 新增项目使用指南和有边界面试 Agent 架构决策记录。
- 历史报告页新增岗位上下文选择器，默认选择最近面试所属岗位，支持切换到具体岗位或"全部岗位"，顶部指标、能力成长曲线、最近表现、知识覆盖和历史记录列表均跟随选中岗位联动过滤。
- 简历画像按岗位隔离存储，同一用户在每个岗位最多保留一份画像，不同岗位画像互不干扰。
- 简历管理页新增岗位选择器，支持按岗位上传 PDF、查看解析画像、覆盖更新和删除画像。
- 面试启动时后端自动按 `userId + positionId` 加载当前岗位的简历定制题，记录使用的简历画像 ID，前端不再负责传递简历定制题。
- 用户可见岗位摘要接口，返回岗位名称、公开/私有标识、历史记录数和按岗位粒度的简历画像状态。
- 知识覆盖支持按岗位查看，RAG 检索日志新增结构化 `position_id`，History 页面和岗位 / 题库维护工作台可按岗位展示知识领域覆盖。
- 恢复本地 `interview-question-bank` skill 和 `scripts/question_bank_import.py`，支持在本机从结构化资料生成题库 JSON 导入包，再回到知识库 / 题库工作台受控导入。
- 知识库 / 题库工作台新增导入包维护区域，支持在可维护岗位知识库中选择 JSON 导入包、校验、导入为草稿、查询、发布、重建索引和归档导入原子。
- 新增知识库 / 题库工作台页面，支持登录用户查看公共岗位与私有岗位、创建和删除私有岗位，并通过后端能力位区分普通用户私有维护和管理员公共维护。
- 支持普通用户在侧边栏配置自己的 OpenAI-compatible 大模型 Provider，并用加密保存的 API Key 驱动面试、报告和 AI Mentor 等用户侧 LLM 功能。
- 新增项目演示视频分镜和中文旁白稿，便于后续制作 GitHub、简历和面试答辩用演示视频。
- 将 120 条 AI 大模型知识原子整理为随仓库发布的内置基础题库，支持本地空库部署时自动导入并参与面试 RAG 检索。
- 知识库 / 题库工作台支持一键发布全部草稿原子，无需逐条手动选择，直接发布当前知识库下所有 DRAFT 状态原子。
- 支持删除私有岗位功能，删除岗位时同步清理关联的知识库、原子、版本记录、源文件和 Qdrant 向量，保留历史面试报告快照不变。
- 历史面试详情新增详细报告区域，支持查看每轮问答、参考答案、来源、单轮估分和详细报告总分。

### 变更
- 浏览器标签页改用 InterWise 品牌图标，替换 Vite 默认图标。
- 公共题库维护页改用正式产品说明，不再在 Web 界面展示特定活动或演示场景措辞。
- 本地 Docker 部署默认启用固定的 `admin / admin123` 管理员登录，关闭邮箱注册、验证码和邮件找回流程；登录页会提示用户自行配置 DeepSeek 等大模型 Provider。
- 本地 Compose 对外端口统一只监听 `127.0.0.1`，SMTP 凭据改为可选；生产 Compose 固定使用邮箱验证模式，避免复用本地环境变量时误启默认管理员。
- 面试下一问统一通过 `InterviewOrchestrator` 产出计划；Agent 关闭或处于非技术阶段时使用稳定规则，首次规划超时仅回退当前轮并在下一技术轮重试，连续第二次超时或其他规划失败后本场保持稳定规则。
- 面试阶段只在流式回复成功完成后与对话历史一起提交；正在生成的轮次与结束、退出操作互斥，避免报告漏掉最后一轮或失败后错误切换阶段。
- AI Mentor 的学习覆盖改为只统计已成功交付并落成结构化面试轮次的知识原子，规划失败、超时或流式失败不再虚增覆盖率。
- 本地 Docker 默认开放账号自有的私有岗位 / 题库维护；所有 `ADMIN` 账号拥有一致的公共题库权限并可新增自己的岗位，普通账号仍按 owner 隔离且不能修改公共或他人私有内容。
- Dashboard、面试准备和设置页改用只读岗位接口获取公共岗位，题库维护关闭不会阻断普通用户选择结构化岗位并开始面试。
- 面试准备页只接受只读可见岗位接口返回的真实岗位 ID；加载失败、空列表或隐藏岗位深链接会安全阻止开始面试并给出明确提示。
- 题库后端服务拆分为搜索、导入、目录查询、发布归档和向量同步等职责模块，保留原 `QuestionBankService` 对外入口，降低后续题库维护和排查成本。
- 历史报告页脚本逻辑拆分为数据加载、图表渲染和详情抽屉 composable，减少单页复杂度并便于后续独立测试。
- 历史列表接口支持可选 `positionId` 参数，按结构化岗位过滤已评分面试记录；无 `positionId` 的历史行仅在"全部岗位"视图可见。
- 简历 API 强制要求 `positionId`，不传返回 400 提示选择岗位，废弃全局单例简历画像模式。
- Dashboard 简历管理入口简化为路由到 `/resume` 岗位简历管理页，准备页按岗位加载简历状态。
- `resume_profile` 表新增 `position_id` 字段，唯一键从 `(user_id)` 改为 `(user_id, position_id)`；`interview_record` 表新增 `resume_profile_id` 字段记录面试启动时使用的画像。
- 历史页面"较上一场"比较在同一岗位视图下使用同岗位上一场记录，在"全部岗位"视图下使用全局上一场记录。
- 内置公共 starter 题库统一改为公共导入包格式导入，Java 后端、Web 前端和 AI 大模型岗位不再依赖旧 `knowledge_base/atoms` 小文件目录；AI 大模型岗位改用细分分类导入包。
- 改进题库切块脚本，按内容切块。
- 用户自有题库维护主路径调整为本机 skill 生成导入包、应用内导入为草稿并人工审查；私有 owner 和公共管理员均可在面板中显式发布、重建索引和归档各自作用域内的原子。
- 面试开始时绑定结构化 `position_id` 并要求该岗位已有已发布且已同步的题库原子；面试 RAG 的 Qdrant 与 MySQL fallback 检索均按岗位作用域过滤，禁止无作用域面试检索。
- 全新空库启动时，内置 legacy JSON 题库按公共岗位分组后以公共知识库作用域导入，避免公共 starter 内容缺少 `position_id`。
- 结束面试恢复为同步生成初步报告并立即展示；`GENERATE_REPORT` 异步任务改为后台生成历史记录中的逐轮详细报告。
- 历史详细报告改为调用用户配置的大模型生成综合参考答案和逐题评分，不再把召回原子直接堆砌为参考答案；逐题评分按相关性、正确性、深度、实践性和表达五项细则给出。
- 管理员可在同一知识库 / 题库工作台维护公共 starter 题库，普通用户只能维护自己的私有岗位题库。
- 知识库 / 题库工作台响应不再返回历史源文件列表，当前 MVP API 模型聚焦岗位、知识库、导入包和原子维护能力位。
- 下线旧 LLM 每日额度限制链路，面试、简历解析和 AI Mentor 不再经过应用内 quota 检查。
- Settings 管理员区移除旧 Question Bank Admin 导入包面板，题库维护入口收口到知识库 / 题库工作台和 `ADMIN` 角色授权流程。
- 移除旧 `/api/admin/question-bank/**` 导入包管理路由，避免绕过用户自有题库工作台继续走开发者后台产品路径。
- 移除未被运行时读取的 `APP_ADMIN_TOKEN` 示例配置和 Compose 透传，部署不再需要配置旧管理令牌。
- Qdrant 题库向量 payload 增加作用域、所有者、岗位、知识库、源文件、发布状态和向量状态字段，便于后续按私有/公共题库边界过滤检索。
- 题库后台和运营统计的产品授权从 `APP_ADMIN_TOKEN` 切换为登录用户 JWT 与 `ADMIN` 角色校验，前端不再要求输入管理令牌。
- README 效果展示新增大模型配置界面截图，并移除项目首页中的多 Agent 工作流说明，使 README 聚焦产品能力和本地部署。
- README 补充内置基础题库边界说明，明确云端私有扩展题库和临时导入包不会自动同步到他人本地部署。
- README 补充内置 `interview-question-bank` skill 的使用说明，方便本地部署者维护自己的私有题库。
- README 效果展示新增题库维护截图，并修复 AI Mentor 截图路径中空格导致的渲染问题。
- 加固面试与简历链路的错误处理，避免将后端、PDF 解析或模型供应商异常原文返回给前端或写入业务事件。
- 前端 Markdown 渲染统一改为 `marked` + DOMPurify 净化后再进入 `v-html`，降低面试官回复和报告内容的 XSS 风险。
- 知识库 / 题库工作台的岗位操作将"归档岗位"替换为"删除岗位"，删除时同步清理关联资源和 Qdrant 向量。
- 文字/视频面试结束页不再等待详细报告异步任务，完成初步报告后提示用户稍后到历史记录查看详细报告。
- Docker 启动期内置公共题库向量同步改为后台异步执行，后端容器可先完成启动，再由单线程任务重试并逐步补齐 Qdrant。

### 修复
- 修复右侧工作区滚动后简历画像、历史报告、面试准备、AI Mentor、设置和大模型配置页的局部标题栏悬浮遮挡正文问题；页面标题现在随工作区内容正常滚动。
- 修复面试报告与面试工作区同时渲染导致双滚动条和布局失衡的问题；文字与视频面试结束后会卸载原工作区，再以全屏遮罩展示报告。应用侧栏和顶部栏固定，仅右侧工作区滚动，并增强浅色报告中雷达图文字、网格和数据区域的对比度。
- 修复管理员可查看和重试其他账号私有题库异步作业的问题；管理员现在仅能处理公共作业和自己的私有作业。
- 修复 Agent 规划偶发超过 20 秒后整场立即固定回退的问题：默认规划预算提高到 35 秒，每轮优先只调用一个最相关工具，并记录不含 Provider 原始异常的延迟与回退状态日志。
- 修复详细报告异步生成失败后历史详情页没有重试入口的问题，失败报告现在可直接重新提交生成任务。
- 优化详细报告生成稳定性：报告 LLM 调用改为独立超时配置，并按小批次生成逐轮评分，降低长面试一次性分析超时风险。
- 清理旧 `common` 软技能原子和被新 AI 大模型导入包替换的粗分类原子，避免内置技术岗位继续混入非技术或过期公共题库内容。
- 修复 Docker 后端部署因 Flyway migration 版本号复用导致 V15 校验和不匹配的问题，恢复 V15-V17 历史迁移并将 RAG 日志 `position_id` 迁移顺延为 V18。
- 修复知识覆盖接口可通过任意私有 `positionId` 暴露他人岗位分类分布的问题，并限制工作台覆盖端点只允许私有 owner 或公共管理员访问。
- 修复岗位 / 题库维护工作台在岗位尚无面试命中时不展示已发布分类结构的问题，零命中岗位会显示 covered 为 0 的覆盖面板。
- 修复前端显示问题。
- 修复已有旧公共题库原子在升级到公共岗位 / 知识库模型后缺少 `position_id`、`knowledge_base_id` 和发布状态归属的问题，启动时会幂等补齐归属并重建对应 Qdrant 向量 payload。
- 修复自定义私有岗位面试 RAG 仍尝试走旧岗位分类映射的问题，结构化岗位、知识库和所有者作用域存在时直接按作用域检索。
- 结束面试报告路径不再依赖前端长轮询，避免空响应、超时或页面离开时遗留 loading、提前清理本地面试状态或继续写入已卸载页面。
- 已完成面试再次结束时会补建缺失的详细报告任务，避免初步报告已写入但异步任务创建失败后无法恢复。
- 历史详情中失败的详细报告会显示失败原因，不再误提示为仍在生成或尚无明细。
- 私有导入包原子在落库时自动加入知识库命名空间，避免全局 `atom_id` 唯一键导致跨用户或跨知识库覆盖。
- 面试 RAG 检索和开始前可用性计数同步使用 `scope`、`position_id` 与 `knowledge_base_id` 过滤，并要求启动请求显式传入结构化岗位 ID。
- 异步详细报告生成会复用已生成的初步报告，并写入 `interview_report` 与基于结构化 `interview_turn` 的逐题 item。
- 旧单原子发布接口同步复用私有 owner / 公共管理员权限校验，避免绕过工作台作用域边界。
- 修复 Docker 新库启动后浏览器残留旧 JWT 访问 `/api/user/me` 时可能触发空指针的问题，改为要求重新登录。
- 修复普通 API 也接受 URL query token 的问题，仅保留 `/api/interview/chatStream` 对 EventSource 的兼容入口。
- 修复邮箱验证码可无限次错误尝试的问题，验证码改用 `SecureRandom` 生成并在连续错误后锁定。
- 修复简历上传在进入 LLM 解析和 PDFBox 解析前缺少后端文件类型、大小和 PDF 头校验的问题，并限制 PDFBox 使用临时文件解析。
- 修复客户端可伪造 `X-Forwarded-For` 绕过 IP 限流的问题，后端优先使用代理写入的真实 IP，前端 Nginx 不再透传客户端伪造链。
- 修复普通用户可通过资料页绑定开发者白名单邮箱从而自申领开发者身份的问题。
- 修复文字/视频面试启动失败时前端覆盖后端业务错误的问题，题库向量尚在同步或最终同步失败时会展示对应业务提示。

### 移除
- 清理旧应用内知识文件存储 Module、`knowledge_storage` Compose 挂载和 `app_job.sourceFileId` 任务响应字段，避免旧文档上传转换方向继续暴露在运行时接口中。
- 移除 MarkItDown / `document-converter` 服务、后端文档转换 client/handler、默认 Compose 依赖和相关配置。
- 应用内题库文档上传、Markdown 读取和源文件 LLM 原子生成接口改为 410，MVP 主流程统一改为本机 skill 生成 JSON 导入包后再导入。
- 移除应用内 LLM 原子生成 client、作业 handler、作业创建服务、生成 DTO 和对应测试，避免旧 `GENERATE_ATOMS` 任务链在恢复或重试路径中继续执行。
- 移除归档私有岗位接口 `POST /api/knowledge-workspace/positions/{positionId}/archive`，统一改为 `DELETE /api/knowledge-workspace/positions/{positionId}` 物理删除。
- 删除旧 `/api/analytics/quota/me` 用户额度查询路由、quota service/DTO/mapper/异常类型及前端额度展示。
- 删除旧 `APP_QUOTA_*`、`APP_DAILY_*` 示例配置和 Docker Compose 环境变量透传。

## v1.2.0 - 2026-06-07

### 新增
- 新增项目页面展示图，覆盖工作台、面试准备、文字面试、视频面试、简历画像、历史报告、AI Mentor 和偏好设置。
- 新增 RAG 离线 rerank 评测脚本，可在固定 AI 大模型评测集上比较二阶段重排与 `multilingual-e5-base` 基线，并记录当前暂不启用生产 rerank 的评测结论。
- 新增独立 `embedding-service`，Docker 部署默认使用 `intfloat/multilingual-e5-base` 生成中文/多语言向量。
- 新增 AI 大模型岗位 RAG 离线检索评测工具链，用于导出脱敏 query、构建候选池、比较 Embedding 模型、生成预标注并计算可复现指标。
- 新增请求级 RAG 检索日志，记录零命中、失败、检索策略、候选数量与延迟，并通过 `request_id` 关联命中原子日志。
- 新增 AI 大模型岗位入口与 `AI大模型` 题库分类映射，支持 AI 大模型题库发布后参与技术面试 RAG 检索。
- 支持在 Settings 的 Operations 中进行题库导入校验、试运行、正式发布、题库查询、批量归档、恢复发布、批次查看和索引维护。
- 新增面向开发者后台的题库管理 API，复用开发者账号白名单与 `APP_ADMIN_TOKEN` 校验。
- 新增本地 Docker 专用 Nginx 配置，使本地环境可以只启动 Web、后端、MySQL、Redis 和 Qdrant，不依赖独立 MCP 服务。

### 变更
- README 部署区补充当前线上体验地址，并将个人运维手册从公开仓库内容中移除。
- 更新后续推进计划，并在 README 中加入与计划对应的 Todo 列表。
- 将 RAG 链路总结移动到可公开访问的文档路径，修复 README 中的失效链接。
- RAG 向量检索支持动态候选集预算：默认仍召回 Top-20，短技术回答或多技术点混杂回答可临时扩大到 Top-30，提示词上下文上限仍保持独立控制。
- RAG 检索默认扩大到 Top-20 候选集，并默认只向面试提示词注入 Top-10 上下文，兼顾召回和提示词噪声控制。
- 单轮检索统一由独立模块负责查询构造、岗位路由、候选日志和上下文选择；只有成功生成的问题所使用的 Top-10 Atom 才会进入已用列表。
- RAG 上下文选择增加轻量判定层，结合候选人低信息回答与召回分数控制补救追问、切换知识点和 Atom 消耗。
- README 重写为稳定发布首页，突出动态面试 RAG、模拟面试与题库打通、题库工程化运维、离线评测和云端部署边界。
- AI Mentor 知识覆盖率改为以已发布 Atom 为分母、实际进入面试上下文的 Atom 为分子，后台缓存预热改用受管线程池。
- 生产 Qdrant 默认切换到 768 维 `interview_atoms_e5_base` collection，切换 Embedding 模型后需要执行题库全量 reindex。
- 规范题库导入包默认批次号和文件名，生成结果包含分类、模式、时间戳和短随机串，便于维护者识别与归档。
- 题库导入的试运行模式不再写入导入批次、题目或向量索引，避免试运行后正式发布因批次号重复失败。
- 题库索引维护默认提供未同步题目的增量重建，全量重建仅作为需要确认的手动操作。
- 统一前端 token 与认证请求头的获取入口，减少设置页、准备页、首页、简历页和埋点逻辑中分散的认证拼接。

### 移除
- 完整下线 MCP 服务、用户 token/配额入口及其部署子模块，生产运行仅保留应用与题库 RAG 链路；原 MCP 记录表以 `retired_mcp_` 前缀归档保留。
- 移除题库脚本的直接提交能力，Skill 仅生成 JSON 导入包，发布统一经开发者管理面板完成。
- 移除未被生产面试链路使用的内存向量库、旧文件加载器和旧 `RagRetriever`。

### 修复
- 修复生产环境示例仍使用旧 384 维 collection 的问题，并为 Embedding、Qdrant HTTP 调用增加连接和读取超时。
- 修复 Qdrant 故障被误记为普通零命中、MySQL 回退使用整段对话导致难以命中，以及 collection 维度不一致时继续检索的问题。
- 修复 Top-20 候选全部被提前标记已使用、流式生成失败仍消耗 Atom，以及同一面试记录可并发启动多轮的问题。
- 修复归档或发布时数据库事务与 Qdrant 操作状态可能分叉的问题；失败操作保留可重试状态，增量重建会补偿写入和删除。
- 修复题库版本号并发分配可能撞唯一键的问题。
- 修复 Qdrant 向量服务在 Spring 容器中无法选择注入构造器导致后端启动失败的问题。
- 修复 embedding 服务依赖中不存在的 `uvicorn` 版本号导致生产镜像构建失败的问题。
- 修复 embedding-service 镜像构建在大体积 PyTorch 依赖下载时容易超时的问题，改为固定 CPU 版 PyTorch 并增加 pip 超时与重试。
- 修复题库后台查询接口缺少 `/api` 前缀导致线上查询落到静态资源处理的问题。
- 修复接口返回 401 时只清除 token、未同步清理用户本地缓存的问题。
