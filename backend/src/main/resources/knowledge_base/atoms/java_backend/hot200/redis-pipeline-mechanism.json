{
  "id": "redis-pipeline-mechanism",
  "subject": "Redis Pipeline 机制与原理",
  "category": "hot200",
  "difficulty": "简单",
  "tags": [
    "Redis",
    "性能优化",
    "网络"
  ],
  "content": {
    "principles": "Redis Pipeline 是一种批量执行命令的机制，核心目的是减少客户端与服务端之间的网络往返次数（RTT）。\n原理：客户端将多条命令一次性打包发送给服务器，服务器依次处理所有命令后，将结果批量返回给客户端。整个过程避免了传统的“发送命令-等待响应”的串行等待。\n与事务（MULTI/EXEC）的区别：Pipeline 不保证原子性，也不支持回滚，它纯粹是网络层的优化手段。而原生批量命令（如 MSET、HMGET）虽然更好，但 Pipeline 支持混合执行不同类型的命令（如 SET、DEL、INCR 组合）。\n性能提升：在需要连续执行大量命令（如设置1000个 key）的场景下，使用 Pipeline 可以将网络交互从 N 次减少到 1 次或几次，显著提升吞吐量（从几百 QPS 提升到几万 QPS）。\n代码示例（Jedis）：\n```java\nPipeline pipeline = jedis.pipelined();\nfor (int i = 0; i < 1000; i++) {\n    pipeline.set(\"key:\" + i, \"value\" + i);\n    pipeline.incr(\"counter\");\n}\nList<Object> results = pipeline.syncAndReturnAll();\n```\n命令先缓存在客户端本地队列，调用 `syncAndReturnAll()` 时才真正批量发送并等待所有回复。",
    "pitfalls": [
      "误以为 Pipeline 能保证命令的原子性（它不能）。",
      "Pipeline 打包的命令数量过大，可能导致客户端或服务器端内存占用过高，或网络包过大。",
      "混淆 Pipeline 和事务的应用场景。"
    ],
    "follow_up_paths": [
      "Pipeline 和原生批量命令（如 MSET）在性能和原子性上有什么区别？如何选择？",
      "在使用 Pipeline 时，如果其中某条命令执行失败，会影响其他命令吗？（考察 Pipeline 的错误处理机制）"
    ]
  }
}