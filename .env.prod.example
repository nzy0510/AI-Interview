# --- Production compose config ---
DB_NAME=ai_interview_ds
DB_USERNAME=interwise_app
DB_PASSWORD=replace_with_a_long_random_database_password
MYSQL_ROOT_PASSWORD=replace_with_a_different_long_random_root_password

# At least 32 characters. Use a strong random value.
JWT_SIGN_KEY=replace_with_a_long_random_jwt_signing_key_32_chars_min
JWT_EXPIRE_MS=86400000

# Analytics and rate limiting.
APP_ANALYTICS_HASH_SALT=replace_with_a_long_random_analytics_hash_salt
APP_RATE_LIMIT_ENABLED=true
# Developer whitelist for local debugging. Admin access uses user ADMIN role.
APP_DEVELOPER_EXEMPT_USER_IDS=
APP_DEVELOPER_EXEMPT_USERNAMES=
APP_DEVELOPER_EXEMPT_EMAILS=

# Server-side key used to encrypt user-managed provider secrets at rest.
# The project does not provide a fallback provider key for normal users.
APP_LLM_CONFIG_ENCRYPTION_KEY=replace_with_a_strong_encryption_key

# Question bank / RAG. Required for the full production feature set.
QDRANT_ENABLED=true
QDRANT_URL=http://qdrant:6333
QDRANT_COLLECTION=interview_atoms_e5_base
QDRANT_VECTOR_SIZE=768
QDRANT_CONNECT_TIMEOUT_MS=3000
QDRANT_READ_TIMEOUT_MS=5000
APP_EMBEDDING_PROVIDER=http
APP_EMBEDDING_ENDPOINT=http://embedding-service:8000/embed
APP_EMBEDDING_QUERY_PREFIX=query:
APP_EMBEDDING_PASSAGE_PREFIX=passage:
APP_EMBEDDING_CONNECT_TIMEOUT_MS=3000
APP_EMBEDDING_READ_TIMEOUT_MS=10000
APP_RAG_RETRIEVAL_LIMIT=20
APP_RAG_RETRIEVAL_LIMIT_MAX=30
APP_RAG_CONTEXT_LIMIT=10
APP_RAG_HIGH_CONFIDENCE_SCORE=0.70
APP_RAG_MIN_CONTEXT_SCORE=0.55
QUESTION_BANK_SEED_FROM_JSON=true
QUESTION_BANK_REINDEX_ON_STARTUP=true
APP_QUESTION_BANK_USER_MAINTENANCE_ENABLED=false

# Mail config. Required for registration and password reset email flows.
MAIL_HOST=smtp.qq.com
MAIL_PORT=587
MAIL_USERNAME=replace_with_your_email
MAIL_PASSWORD=replace_with_your_smtp_authorization_code

# No domain yet: keep the application on loopback and use an SSH tunnel.
# Example: ssh -N -L 18080:127.0.0.1:8080 your-server
APP_CORS_ALLOWED_ORIGINS=http://127.0.0.1:18080

# Optional HTTPS reverse proxy. Keep the frontend off the public interface.
# When enabling the https profile, set:
# DOMAIN_NAME=your-dns-name
# FRONTEND_HTTP_BIND=127.0.0.1:8080
DOMAIN_NAME=
FRONTEND_HTTP_BIND=127.0.0.1:8080

# Optional public ICP record. Rebuild the frontend image after changing this build-time value.
VITE_ICP_RECORD=
# Optional approved public security record, including province prefix and numeric code.
VITE_PUBLIC_SECURITY_RECORD=

# Required only with docker-compose.images.yml; use your verified build tag.
INTERWISE_IMAGE_TAG=
# Optional independent frontend release; defaults to INTERWISE_IMAGE_TAG.
INTERWISE_FRONTEND_IMAGE_TAG=
