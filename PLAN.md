# InterWise 后续推进计划

## 目标

把当前稳定可用的 `v1.2.0` 版本继续推进为更适合公开展示、内测转化和长期维护的产品。优先顺序不再围绕“能不能跑起来”，而是围绕用户体验、RAG 质量、部署可靠性、数据安全和后续工程化。

## Phase 1：公开展示与项目可信度

- 优化 README、Release Notes 和展示图，让访问 GitHub 的用户能快速理解项目定位、功能闭环和创新点。
- 在 README 中保留线上体验地址，降低试用门槛。
- 保持公开文档聚焦项目能力，个人运维手册和服务器操作细节不上传 Git。
- 持续补充真实页面截图，展示工作台、面试、简历画像、历史报告、AI Mentor、题库管理等核心页面。

验收标准：

- README 中能看到线上地址、核心创新、架构图、RAG 流程、快速启动和后续计划。
- GitHub 仓库不暴露个人服务器运维细节、密钥路径或仅供个人使用的操作说明。

## Phase 2：动态 RAG 质量评测与 rerank 决策

- 维护固定 AI 大模型岗位评测集，避免每次模型或参数调整后只能凭主观体验判断效果。
- 用现有离线评测工具链持续比较 embedding、候选集大小、阈值和 rerank 策略。
- 当 rerank 指标稳定优于当前 multilingual-e5 基线，再设计生产接入方式。
- 生产接入前明确延迟、成本、失败降级和可观测性要求。

验收标准：

- 至少保留一套固定评测集和对应指标报告。
- rerank 是否上线由评测指标和延迟成本共同决定，而不是单次样例效果。

## Phase 3：题库质量与岗位覆盖扩展

- 继续完善 Java 后端、前端、AI 大模型、HR 软技能等分类题库。
- 对高频岗位补充更细的标签、难度、考察点和 followUpPath。
- 题库新增仍通过导入包、Question Bank Admin 校验、发布和 reindex，不直接改库。
- 定期抽样检查 RAG 命中内容是否真正适合作为追问依据。

验收标准：

- 新增题库均可追踪导入批次、发布状态和 Qdrant 同步状态。
- 发布后 Qdrant points 数量与已发布 Atom 数量一致。

## Phase 4：产品体验与面试闭环增强

- 优化面试准备页，使岗位、难度、重点方向和简历画像的关系更清晰。
- 优化面试报告和 AI Mentor，让用户知道下一步该练什么。
- 改进视频面试体验，包括权限提示、失败提示和弱网场景下的降级。
- 补充更清晰的反馈入口，便于收集内测问题。

验收标准：

- 用户能从准备、面试、报告、Mentor 建议形成完整训练闭环。
- 关键失败场景有可理解提示，而不是只表现为空白或 500。

## Phase 5：部署可靠性与数据保护

- 保持当前 Azure VM + Docker Compose 的单机部署方案，直到真实用户量需要拆分架构。
- 对 MySQL、uploads 和必要的 Qdrant 数据建立可恢复备份流程。
- 保持 `/api/health`、容器状态、磁盘空间和 Qdrant collection 状态的定期检查。
- 本地部署继续向云端逻辑对齐，但不把模型缓存、数据库数据、服务器运维说明提交到 Git。

验收标准：

- 可以明确回答最近一次备份在哪里、如何恢复、恢复后如何重建 Qdrant 索引。
- 生产环境健康检查不只看容器 `Up`，还要看后端依赖和向量索引状态。

## Phase 6：安全、隐私与成本控制

- 持续检查 `.env`、密钥、服务器路径和个人运维信息是否被误提交。
- 保持注册、登录、验证码、AI 面试、简历解析和 Mentor 生成的限流与每日额度。
- 增加面向用户的隐私说明，说明简历、面试记录和 AI 分析数据的用途。
- 后续补充用户数据删除能力，如删除简历画像、删除面试记录。

验收标准：

- 公开仓库不包含密钥、个人服务器敏感路径或生产数据。
- 高频请求不会无限消耗 LLM、SMTP 和服务器资源。

## 推荐执行顺序

1. 完成公开 README、展示图、Release 和文档边界整理。
2. 固化 RAG 评测流程，并形成 rerank 是否上线的证据。
3. 扩充题库质量和岗位覆盖。
4. 优化面试报告、Mentor 和视频面试体验。
5. 完善备份、健康检查和本地/云端部署一致性。
6. 补充隐私说明、数据删除和更细粒度成本控制。
